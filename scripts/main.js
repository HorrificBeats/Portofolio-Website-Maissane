/* function clicked() {
    if(document.getElementsByClassName('Clicked').classList.contains('active')) {
        document.getElementsByClassName('Clicked').classList.remove('active');
    } else {
        document.getElementsByClassName('Clicked').classList.add('active');
    }   
} */


/* window.onload

window.onload = function() {
    var input = document.getElementById("myCarousel").focus();
  }

  element.click() */

/*   $(document).keydown(function(e) {
    if (e.keyCode === 37) {
       // Previous
       $('.carousel').carousel('prev');
       return false;
    }
    if (e.keyCode === 39) {
       // Next
       $('.carousel').carousel('next');
       return false;
    }
    });
 */
/**
 * * Highlihght
 * ! Danger
 * ? Questions
 * TODO: blabla
 */
